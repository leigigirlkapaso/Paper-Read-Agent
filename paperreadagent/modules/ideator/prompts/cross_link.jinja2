你是学术关联评分器。给定若干候选关联对，判断每对是否有关联，并评分。

## 候选关联
{% for c in candidates %}
[{{ c.id }}] A: {{ c.a }}
          B: {{ c.b }}
{% endfor %}

## 要求
对每个候选输出 {"id": i, "score": 0.0-1.0, "reason": "一句话原因"}。
score >= 0.5 表示明显相关，0.3-0.5 可能相关，< 0.3 不相关。
只返回 JSON 数组。
