# 当前轮次：第 {{ round_number }} 轮

# 研究火花（spark）

{{ spark_content }}

# 论文事实层（facts_block）

{{ facts_block if facts_block else "（无关联论文）" }}

# 上一轮大纲

{% if current_outline %}
{{ current_outline }}
{% else %}
（首轮，尚无大纲。请基于本轮讨论 + 火花 + 事实层生成完整 7 节大纲。）
{% endif %}

# 本轮 agent 发言（按 seat 顺序）

{% for msg in current_round_msgs %}
## [{{ msg.seat }} / {{ msg.role }}]

{{ msg.content }}

{% endfor %}

# 你的任务

基于上述内容，重写一份完整的 7 节大纲。遵守 system prompt 的硬性约束。直接输出 Markdown 文档，第一字符必须是 `#`。
